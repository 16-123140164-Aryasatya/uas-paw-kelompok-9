[app:main]
use = egg:library_backend

pyramid.reload_templates = true
pyramid.debug_authorization = false
pyramid.debug_notfound = false
pyramid.debug_routematch = false
pyramid.default_locale_name = en

# Database URL
#
# The application will use the value of the ``DATABASE_URL`` environment
# variable when present.  This setting can optionally be set to a
# PostgreSQL URL for local development, but should not point to SQLite
# because SQLite fallback has been removed.  Example:
# sqlalchemy.url = postgresql://user:password@host:port/dbname
sqlalchemy.url = postgresql://postgres:TOvjUnJMuTKAgwzRATvDespbZKFEUGhj@switchback.proxy.rlwy.net:47041/railway

# JWT Secret - ganti dengan random string yang kuat
jwt.secret = ASxyE3Ubek2K4X373F-z5mBXHs95iC2FfepnPgXNA8Y

# Session
session.secret = TfzzzN-oYgitCNSdvK0Yv6CQx8xwoXhriqUH-dcyKSE

# CORS Settings
cors.origins = http://localhost:5173 http://localhost:3000

[server:main]
use = egg:waitress#main
host = 0.0.0.0
port = 6543

[loggers]
keys = root, library_backend, sqlalchemy

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = INFO
handlers = console

[logger_library_backend]
level = DEBUG
handlers =
qualname = library_backend

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(asctime)s %(levelname)-5.5s [%(name)s:%(lineno)s][%(threadName)s] %(message)s